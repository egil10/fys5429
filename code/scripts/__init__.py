"""FYS5429 — importable package (code/scripts on disk).

Library-style modules (import for notebooks / reuse):
  bm, bs, heston, calibrate, greeks, metrics, style, utils,
  pinn_bs, pinn_heston, generate

CLI / pipelines (run via ``python -m fys5429.<module>``):
  run, run_all, pipeline, pipeline_heston, pipeline_calibrate
"""
